services:
  - type: web
    name: bitacora-incidencias
    env: node
    buildCommand: npm install --build-from-source
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
